name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Echo project name
        run: echo "Running workflow for payment-processor"
      - name: Simple build step
        run: echo "Building payment-processor..."